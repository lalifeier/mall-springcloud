spring:
  cloud:
    nacos:
      discovery:
        #        namespace: ${spring.profiles.active}
        server-addr: http://localhost:8848
        username: nacos
        password: nacos
      config:
        #        namespace: ${spring.profiles.active}
        server-addr: http://localhost:8848
        file-extension: yaml
#        extension-configs[0]:
#          data-id: gateway.yaml
#          group: DEFAULT_GROUP
#          refresh: false

logging:
  level:
    root: info
